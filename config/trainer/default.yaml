max_epochs: 51
batch_size: 128
learning_rate: 0.0003
weight_decay: 0.01
optimizer: adamw
lr_scheduler: cosine
lr_warmup_fraction: 0.1
lr_warmup_start_factor: 0.001
lr_min: 0.0
num_workers: 4
device: auto
checkpoint_dir: ${paths.output_dir}/checkpoints
log_level: INFO
log_file: ${paths.output_dir}/train.log
evaluate_test: true
