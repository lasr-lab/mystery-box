# ML Package

This package owns training, evaluation, model definitions, data loading, and inference utilities for DIGIT tactile fabric classification.

Start training through the Hydra entry point:

```bash
python -m src.ML.train
```
