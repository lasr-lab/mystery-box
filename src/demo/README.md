# Demo Package

This package owns the web interface for the DIGIT tactile sensor demonstration.

The current entry point is a placeholder that loads Hydra config and prints the configured classes:

```bash
python -m src.demo.app
```
